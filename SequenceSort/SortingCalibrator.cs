using System;
using System.Collections.Generic;
using System.Text;

namespace SequenceSort
{
    public class SortingCalibrator
    {
        public int K { get; internal set; } = 400;

        public SortingCalibrator()
        {

        }

        // This method will execute a series of sorting algorithms and analize the efficiencies when given a sequence length of size N. Results are used for dynamically changing sorting execution.
        internal void Calibrate()
        {

            throw new NotImplementedException();
        }
    }
}
